<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron/V8 needs writable+executable memory and JIT. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- koffi loads native dylibs via FFI and we spawn bundled third-party
       binaries (ffmpeg/whisper-cli); both require relaxed library validation
       and dyld env vars under the hardened runtime. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Microphone capture (ffmpeg avfoundation child process). -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
